<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/addepi"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/panel_white">

    <com.remember.app.customView.CustomAutoCompleteEditText
        android:id="@+id/text_epitaph"
        style="@style/TextAreaStyle"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:hint="@string/text_epitaph"
        android:maxLength="1024"
        android:textColor="?android:attr/textColor"
        android:textSize="20dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.remember.app.customView.CustomButton
        android:id="@+id/save_button"
        style="@style/CustomAppButtonStyle"
        android:background="@drawable/button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:text="@string/save"
        android:textAllCaps="false"
        android:textSize="20dp"
        app:layout_constraintTop_toBottomOf="@+id/text_epitaph"
        tools:ignore="MissingConstraints"
        tools:layout_editor_absoluteX="0dp" />

</androidx.constraintlayout.widget.ConstraintLayout>