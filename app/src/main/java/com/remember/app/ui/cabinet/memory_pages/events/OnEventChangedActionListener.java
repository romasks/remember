package com.remember.app.ui.cabinet.memory_pages.events;

import com.remember.app.data.models.EventModel;

public interface OnEventChangedActionListener {

    void onEventChanged(EventModel eventModel);
}
